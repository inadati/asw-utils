---
name: pov-harness
description: |
  This skill should be used when the user asks to "pov-harnessを起動して", "審査委員会を立ち上げて",
  "PoVハーネスを開始して", "観点駆動で開発して",
  or wants to run implementation with multi-perspective review committee evaluation.
  PoV = Point of View（観点）。
version: 0.1.0
tools: Read, Write, Edit, Bash, Agent, AskUserQuestion
---

# pov-harness スキル（準備中）

PoV（観点）駆動の開発ハーネス。

実装プラン完成後に審査委員会（複数のPoVエージェント）を編成し、
実装→多観点評価→修正のループで品質を担保する。

## 現在のステータス

このスキルは現在開発中です。

## 設計概要

```
wantree（要件定義）
    ↓
実装プランニング（プランモード）
    ↓
plan-review ─── 実装プランをステップバイステップでレビュー
    ↓
実装プラン確定
    ↓
審査委員会プランニング（プランモード）
  ├── 汎用テンプレートを参照
  └── 確定した実装プランを踏まえてPoV構成を提案
    ↓
plan-review ─── 審査委員会プランをステップバイステップでレビュー
    ↓
審査委員構築（PoVエージェント起動）
    ↓
実装 / 修正  ←────────────────────┐
    ↓                              │
審査委員会による並列評価            │
（各PoVエージェントが独立して採点）  │
    ↓                              │
審査委員長による集約・調停・判定          │
    ├── 合格ライン達成 → 実装完了           │
    └── 未達あり → 差し戻し ───────────────┘
```

## plan-reviewが2回走る理由

| 回 | 対象 | 目的 |
|----|------|------|
| 1回目 | 実装プラン | 技術的方針・設計の確認 |
| 2回目 | 審査委員会プラン | PoV構成・合格ラインの確認 |

実装プランが確定した後でないと審査委員会の構成が決められないため、
必ず直列で実行する。

## 審査委員会プランニングの入力

プランモードで以下の2つを参照して委員会構成を提案する：

1. **汎用テンプレート** (`references/default-committee.yml`) ─── ベースとなる観点一覧
2. **確定した実装プラン** ─── プロジェクト固有の観点を追加・削除する根拠

## 合格ラインの設計

### スコア体系

- 審査員1人あたり **独立した100点満点**
- 総合満点 = 観点の数 × 100（参考値）
- 合格の判定は **各エージェントの個別スコア** で行う（総合点での判定ではない）

### 競合問題と解決策

セキュリティとUXが競合する場合、セキュリティエージェントは「UXの要件のせいで
自分の観点では70点しかつけられない」という状況が生じる。
このとき総合点の閾値を下げても意味がない。セキュリティが70点という事実は変わらないため。

**正しい解決策**: 競合するPoV同士は**それぞれの合格点を個別に設定**する。

### 合格ラインの提案ルール

審査委員会プランニング時にPoV間の競合を分析し、各エージェントの合格点を個別に提案する：

| 状況 | 合格点の設定 |
|------|------------|
| 競合なし | 全員 **100点** |
| 競合あり | 競合するPoVの合格点を個別に引き下げ、それ以外は100点のまま |

### 競合と判断する組み合わせ例

- セキュリティ vs UX（制約 vs 利便性）
- パフォーマンス vs 保守性（最適化 vs 可読性）
- 機能実現度 vs デザイン統一度（スピード vs 品質）

### 合格ラインの提案フォーマット

審査委員会プランに以下を含めること：

```yaml
passing_criteria:
  members:
    - role: セキュリティ安全度
      passing_score: 70   # UXと競合するため引き下げ
    - role: ユーザー要望実現度
      passing_score: 80   # セキュリティ制約により一部妥協
    - role: 保守性
      passing_score: 100  # 競合なし
    - role: パフォーマンス
      passing_score: 100  # 競合なし
  reason: "セキュリティとUXが競合するため、該当観点の合格点を個別に引き下げ"
```
